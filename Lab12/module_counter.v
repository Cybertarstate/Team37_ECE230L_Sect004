module module_counter (
    input clk,
    input reset,
    output [2:0]s,
    output y
);

    if (clk) begin
    end 
    
    if (reset) begin
    end
    
    d_FF inst (
    
    );
    
    full_adder inst (
    
    );
    
    // Possibly a for loop?
    always @(posedge clk) begin 
    
    end

endmodule